services:
  zirag:
    build: .
    ports:
      - "7860:7860"
    volumes:
      - ./cache:/app/cache
      - ./data:/app/data
    env_file:
      - .env
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
